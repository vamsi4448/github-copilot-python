# Sudoku Flask Starter

A starter Flask Sudoku application with puzzle generation, hint support, and a browser-based frontend.

## Testing

Run the complete test suite with:

```bash
python -m pytest -q
```

Be sure to run the full test suite after making any changes to the application.
